---
import { profileConfig } from "../config";
import { url } from "../utils/url-utils";

const currentYear = new Date().getFullYear();
---

<!--<div class="border-t border-[var(&#45;&#45;primary)] mx-16 border-dashed py-8 max-w-[var(&#45;&#45;page-width)] flex flex-col items-center justify-center px-6">-->
<div class="transition border-t border-black/10 dark:border-white/15 my-10 border-dashed mx-32"></div>
<!--<div class="transition bg-[oklch(92%_0.01_var(&#45;&#45;hue))] dark:bg-black rounded-2xl py-8 mt-4 mb-8 flex flex-col items-center justify-center px-6">-->
<div class="transition border-dashed border-[oklch(85%_0.01_var(--hue))] dark:border-white/15 rounded-2xl mb-12 flex flex-col items-center justify-center px-6">
    <div class="footer-meta transition text-sm text-center">
        <span class="footer-force-black">&copy; <span id="copyright-year">{currentYear}</span> {profileConfig.name}. All Rights Reserved. /</span>
        <span class="feed-link"><a class="transition link font-medium" target="_blank" href={url("rss.xml")}>RSS</a></span> /
        <a class="transition link font-medium" target="_blank" href={url("sitemap-index.xml")}>Sitemap</a> /
        <span class="beian"><a class="transition link font-medium" target="_blank" href="https://icp.gov.moe/?keyword=20260427">萌ICP备20260427号</a></span><br>
        <span class="footer-force-black">Powered by</span>
        <a class="transition link font-medium footer-link-astro" target="_blank" href="https://astro.build">Astro</a> &
        <a class="transition link font-medium footer-link-fuwari" target="_blank" href="https://github.com/saicaca/fuwari">Fuwari</a>
        <br>
        <span class="uptime-line cursor-pointer select-none" title="戳一下，看板娘会汇报运行时间哦">
            <span class="footer-force-black">本站已运行：</span>
            <span class="uptime-days">0</span>天
            <span class="uptime-hours">0</span>小时
            <span class="uptime-minutes">0</span>分
            <span class="uptime-seconds">0</span>秒
        </span>
    </div>
</div>

<script is:inline>
    (function () {
        if (window.__uptimeInitialized) return;
        window.__uptimeInitialized = true;

        const start = new Date('2026-02-11T08:58:18');

        function getUptime() {
            const diff = Date.now() - start.getTime();
            const totalSeconds = Math.max(0, Math.floor(diff / 1000));
            return {
                days: Math.floor(totalSeconds / 86400),
                hours: Math.floor((totalSeconds % 86400) / 3600),
                minutes: Math.floor((totalSeconds % 3600) / 60),
                seconds: totalSeconds % 60,
            };
        }

        function update() {
            const u = getUptime();
            document.querySelectorAll('.uptime-days').forEach(el => el.textContent = String(u.days));
            document.querySelectorAll('.uptime-hours').forEach(el => el.textContent = String(u.hours));
            document.querySelectorAll('.uptime-minutes').forEach(el => el.textContent = String(u.minutes));
            document.querySelectorAll('.uptime-seconds').forEach(el => el.textContent = String(u.seconds));
        }

        function speak() {
            const show = window.showWaifuMessage;
            if (typeof show !== 'function') return;
            const u = getUptime();
            const templates = [
                `本站已经运行了 ${u.days} 天 ${u.hours} 小时 ${u.minutes} 分 ${u.seconds} 秒啦～`,
                `我已经在这里站了 ${u.days} 天岗，厉害吧！`,
                `时间过得好快，已经 ${u.days} 天 ${u.hours} 小时了呢。`,
                `再点一下，我也不会累的哦。`,
            ];
            const msg = templates[Math.floor(Math.random() * templates.length)];
            show(msg, 4000, 8);
        }

        update();
        setInterval(update, 1000);
        document.addEventListener('click', function (e) {
            if (e.target.closest('.uptime-line')) speak();
        });
    })();
</script>


<style>
  .footer-force-black {
    color: #6b7280 !important;
    text-shadow: none !important;
  }

  .footer-meta {
    color: var(--btn-content);
    color: color-mix(in oklab, var(--btn-content) 90%, black 10%);
    line-height: 1.75;
    text-shadow: 0 1px 2px rgb(0 0 0 / 0.28), 0 0 1px rgb(255 255 255 / 0.24);
  }

  :global(html.dark) .footer-meta {
    color: color-mix(in oklab, var(--btn-content) 92%, white 8%);
    text-shadow: 0 1px 3px rgb(0 0 0 / 0.6);
  }

  .footer-meta a {
    color: inherit;
    opacity: 0.92;
    text-decoration: none;
    text-shadow: 0 1px 2px rgb(0 0 0 / 0.26), 0 0 1px rgb(255 255 255 / 0.22);
  }

  .footer-meta a:hover {
    opacity: 1;
  }

  :global(html.dark) .footer-meta a {
    text-shadow: 0 1px 3px rgb(0 0 0 / 0.58);
  }
</style>
